query GetSolicitudesEliminacionById(
    $id: ID!
){
    solicitud(
        id: $id
    ) {
        id
        titulo
        motivo
        fecha
        estadoActual
        esSpam
        idHecho
        creador
    }
}